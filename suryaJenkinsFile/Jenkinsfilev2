pipeline {
    agent any
    environment {
        name = "surya ganesh"
    }
    stages{

        stage('hello world'){
            steps{
                echo 'hello world .'
                sh 'ls -lrt'
                echo "${env.name}"
            }
        }
    }
}